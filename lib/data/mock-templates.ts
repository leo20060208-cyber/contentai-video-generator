import { getAllTemplates } from './template-library';

/**
 * Mock templates - Ahora usa la librería central
 * @deprecated Use getAllTemplates() from template-library instead
 */
export const mockTemplates = getAllTemplates();
